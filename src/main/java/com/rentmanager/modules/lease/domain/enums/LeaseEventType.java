package com.rentmanager.modules.lease.domain.enums;

public enum LeaseEventType {

    LEASE_CREATED,
    LEASE_APPROVED,
    LEASE_ACTIVATED,
    LEASE_RENEWED,
    LEASE_EXPIRED,
    LEASE_TERMINATED,
    LEASE_CANCELLED
}